code,batch,what
# REMOVED FROM THE REPOSITORY 2026-09-20.
# This file held 57 live redeemable gift codes (11 of them lifetime Plus) and
# this repository is public, so every one of them was readable by anyone who
# found the URL. Minted codes are credentials: they are worth money and they
# belong nowhere near a public tree.
#
# The real file now lives at agents/billing/private/starter_vouchers.csv,
# which .gitignore keeps out of git. Mint future batches there.
#
# NOTE: git history still contains the old codes. Removing the file does NOT
# make them safe. The only real fix is to retire the batches in D1 and mint new
# ones - see agents/billing/private/README-ROTATE.txt.
